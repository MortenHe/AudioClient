import { PlaylistSearchPipe } from './playlist-search.pipe';

describe('PlaylistSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new PlaylistSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
